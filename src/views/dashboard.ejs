<%- include('partials/header') -%>
    <p>Hello <%= username %></p>
    <div class="box">
        <% guilds.forEach(guild => { %>
            <%- include('partials/guild', {guild}) -%>
        <% }); %>
    </div>
    <div class="my-logout-button">
        <form action="/auth/logout">
            <button class="button is-danger is-rounded is-large custom-button" type="submit">Logout</button>
        </form>
    </div>
<%- include('partials/footer') -%>